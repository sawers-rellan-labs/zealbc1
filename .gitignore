# scratch / traceability (holds copied external repos too)
agent/

# large / external data (not version-controlled)
data/

# Nextflow runtime
.nextflow/
.nextflow.log*
work/
timeline*.html
report*.html
trace*.txt

# Slurm job logs (head-job stdout/err; the wrappers route these to ZEAL/results/log)
*.out
*.err

# OS / editor
.DS_Store
*.swp
