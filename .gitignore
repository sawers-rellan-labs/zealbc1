# scratch / traceability (holds copied external repos too)
agent/

# large / external data (not version-controlled)
data/

# Nextflow runtime
.nextflow/
.nextflow.log*
work/
timeline*.html
report*.html
trace*.txt

# Slurm job logs + conda/phg env-build byproducts (were cluttering the worktree root)
*.out
*.err
*.log
phg_environment.yml
phg_tiledb_environment.yml
logs/*
!logs/.gitkeep

# OS / editor
.DS_Store
*.swp

# rendered Quarto notebooks (embed images from gitignored agent/)
docs/notebooks/*.html
docs/notebooks/*_files/
