# R analysis env (heavy -> its own env). bcftools included because build_hd.R queries the VCFs.
channels:
  - conda-forge
  - bioconda
dependencies:
  - r-base=4.4
  - r-data.table
  - r-optparse
  - bcftools=1.21
